import React from 'react';
import {observer} from 'mobx-react-lite';
import {BottomTabNavigator} from '../BottomTabNavigator';

export const BottomTabBinding = observer(() => {
  return <BottomTabNavigator />;
});
